# Generated listening audio

Files in this directory (`*.mp3`) are produced by
`npm run generate:audio` from `data/questions/part{1-4}/*.json` and are
gitignored. Run the script after adding new listening questions.
